# 06 COTRACKER

Meta's CoTracker3 package and its weights (`checkpoints/scaled_offline.pth`,
`checkpoints/scaled_online.pth`, CC BY-NC 4.0). Not in git; `SETUP.bat`
restores it from the GitHub release.
