# 00 PYTHON

Bundled Python 3.11 with PyTorch (CUDA), PySide6 and the rest of the app's
packages. Not in git: it is over 4 GB. Run `SETUP.bat` at the repository root
to download it from the GitHub release and unpack it here.
